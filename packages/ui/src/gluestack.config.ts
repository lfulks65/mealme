/**
 * Gluestack UI v2 + NativeWind configuration
 *
 * Re-exports the default gluestack theme config with
 * NativeWind (Tailwind CSS) tokens for cross-platform styling.
 */
export { gluestackUIConfig as gluestackConfig } from '@gluestack-ui/config';
